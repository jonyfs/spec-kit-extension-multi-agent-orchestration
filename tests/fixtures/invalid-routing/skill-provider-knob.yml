---
schema_version: "1.0"
skill: "speckit.specify"
routing:
  provider_env: "P"
  model_env: "M"
  reasoning_effort: "high"
  reasoning_budget: 4000
  ui_render: {color: "#27AE60", terminal_ansi: "32"}
governance:
  token_guardrails: {max_total_tokens: 80000, action_on_limit: "halt"}
execution_strategy: {parallelism_mode: "serial"}
artifacts: {output: "c.md", writes_artifacts: true}
